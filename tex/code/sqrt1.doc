x_n = sp.var('x_n')
md('`NewtonRaphson`関数は、方程式 $', F(x), '=', 関数式, ''= 0$ の根をNewton-Raphson法を用いて求める関数を返します。この手法は、方程式の根の近似値 $x_n$ を漸次改善します。このために、$(x_n, f(x_n))$ におけるTaylor展開の一次近似を用います：\n\n
$$''', NEWTON_RAPHSON, '$$\n\n',
'関数式の定義 $', 関数式, '''$ をこれにあてはめて，以下の差分計算が得られます：\n\n
$$x_{n+1} = ''', 差分計算式.subs(x, x_n), '$$')
